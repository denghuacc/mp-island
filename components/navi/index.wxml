<!-- components/navi/index.wxml -->

<view class="container">
  <image bind:tap="handleLeft" class="icon" src="{{isLatest ? disLeftSrc: leftSrc}}" />
  <text class="title">{{title}}</text>
  <image bind:tap="handleRight" class="icon" src="{{isFirst ? disRightSrc: rightSrc}}" />
</view>
