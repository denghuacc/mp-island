<view class="container">
  <image bind:tap="onLeft" class="icon" src="{{isLatest ? disLeftSrc: leftSrc}}" />
  <text class="title">{{title}}</text>
  <image bind:tap="onRight" class="icon" src="{{isFirst ? disRightSrc: rightSrc}}" />
</view>